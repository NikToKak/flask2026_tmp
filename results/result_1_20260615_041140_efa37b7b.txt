int sum_list(int *arr, int len) {
    double total;
    double x;
    total = 0;
    for (int i = 0; i < len; i++) {
        int x = arr[i];
        total = (total + x);
    }
    return total;
}