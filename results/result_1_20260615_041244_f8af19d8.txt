int main() {
    double greeting;
    double input;
    double name;
    double print;
    name = malloc(256);
    fgets(name, 256, stdin);
    { char *p = strchr(name, '\n'); if (p) *p = 0; }
    greeting = malloc(strlen("Hello, ") + strlen(name) + 1);
    strcpy(greeting, "Hello, ");
    strcat(greeting, name);
    printf("%s\n", greeting);
}